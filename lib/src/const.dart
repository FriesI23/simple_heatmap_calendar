import 'package:flutter/material.dart';

const defaultCellBorderRadius = BorderRadius.all(Radius.circular(2.0));
const defaultCellSize = Size.square(24.0);
const defualtCellSpaceBetween = 2.0;
const defaultCellValuePadding = EdgeInsets.all(4.0);

const defaultWeekLabelDateFormatter = 'ccccc';

const maxDayOfWeek = DateTime.sunday;
